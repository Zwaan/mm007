function F = lagrange( X,Y )
%LAGRANGE 
%determs function via lagrange
%  X=rowvector of X-values e.g. [3 4 5 6]
%   Y=rowvector of Y-values e.g. [66 77 88 123]

syms x;
k=length(X);
Z(1,1:k)=x;T=Z-X;
F=0;
for l=1:k
    NX(1,1:k)=X(l);N=NX-X;
    F=F+Y(l)*prod(T(cat(2,1:l-1,l+1:k)))/prod(N(cat(2,1:l-1,l+1:k)));%first 
    F=expand(F);
end
end